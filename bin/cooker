#!/usr/bin/env bash
# ONUR AI Dedicated Agent Launcher: cooker
export PATH="$HOME/.opencode/bin:$HOME/.local/bin:$PATH"

if [ $# -eq 0 ]; then
    exec opencode --agent "cooker" --model "ollama/qwen3-coder:30b"
else
    exec opencode run --agent "cooker" --model "ollama/qwen3-coder:30b" "$@"
fi
