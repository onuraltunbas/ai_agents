#!/usr/bin/env bash
# ONUR AI Dedicated Agent Launcher: doktor
export PATH="$HOME/.opencode/bin:$HOME/.local/bin:$PATH"

if [ $# -eq 0 ]; then
    exec opencode --agent "doktor" --model "ollama/qwen3-coder:30b"
else
    exec opencode run --agent "doktor" --model "ollama/qwen3-coder:30b" "$@"
fi
